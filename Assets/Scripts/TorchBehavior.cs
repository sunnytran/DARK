using UnityEngine;
using System.Collections;

public class TorchBehavior : MonoBehaviour
{
	
	public string id;
	public bool lit = true;
	
	void Start()
	{
		
	}
	
	void Update()
	{
//		PlaySound();
	}
	
	void PlaySound()
	{
//		float dist = Vector3.Distance(transform.position, GameObject.Find("Player").transform.position);
//		AudioSource ts = GameObject.Find("Sound").GetComponent<Sound>().torch;
//		if (dist <= 3)
//		{					
//			if (!ts.isPlaying)
//				ts.Play();
//		}
	}
		
}
